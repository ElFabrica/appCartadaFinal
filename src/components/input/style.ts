import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        color:"#AAAAAa",
        fontSize: 14,
        fontWeight: 600,
        borderWidth: 1,
        borderRadius: 10,
        padding: 6
    }
})